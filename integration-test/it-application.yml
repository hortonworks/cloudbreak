mock:
  server:
    address: test
  image:
    catalog:
      url: https://test:9443/imagecatalog

integrationtest:
    testsuite:
      threadPoolSize: 5
    defaultBlueprintName: hdp-small-default
    outputdir: /it
    command: suites
    cleanup:
      cleanupBeforeStart: false
