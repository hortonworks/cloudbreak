name: Resource_tests
parallel: "instances"
parameters:
  cleanUp: false

tests:
  - name: cluster_definitions_test
    classes:
      - com.sequenceiq.it.cloudbreak.ClusterDefinitionTests

#  - name: cluster_test
#    parameters:
#      provider: gcp
#    classes:
#      - com.sequenceiq.it.cloudbreak.ClusterTestFactory

