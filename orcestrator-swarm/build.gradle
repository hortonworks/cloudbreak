apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

jar {
    baseName = 'orcestrator-swarm'
}

dependencies {
    compile project(':orcestrator-api')
    compile group: 'com.github.docker-java', name: 'docker-java', version: '1.0.0'
}