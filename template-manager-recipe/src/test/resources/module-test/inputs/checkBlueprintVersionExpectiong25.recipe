#!/bin/bash

if {{{#eq clusterdefinition.version '2.5'}}}true{{{else}}}false{{{/eq}}}; then
    echo "The cluster definition\'s version if 2.5"
else
    echo "The cluster definition\'s version is not 2.5!"
fi