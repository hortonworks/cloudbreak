package com.sequenceiq.cloudbreak.cloud.gcp;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

import com.sequenceiq.cloudbreak.cloud.model.Platform;
import com.sequenceiq.cloudbreak.cloud.model.Variant;

public class AbstractGcpResourceBuilderTest {

    private TestAbstractGcpResourceBuilder underTest = new TestAbstractGcpResourceBuilder();

    @Test
    public void testPlatform() {
        Platform platform = underTest.platform();
        Assert.assertEquals(Platform.platform("GCP"), platform);
    }

    @Test
    public void testVariant() {
        Variant variant = underTest.variant();
        Assert.assertEquals(Variant.variant("GCP"), variant);
    }

    @Test
    public void testDescription() {
        Assert.assertEquals("Generated by CDP.", underTest.description());
    }

}