{
  "kafka-broker": {
    "properties": {
      "default.replication.factor": "{{{ general.kafkaReplicationFactor }}}",
      "offsets.topic.replication.factor": "{{{ general.kafkaReplicationFactor }}}"
    }
  }
}