{
{{{#if-true sharedService.attachedCluster}}}
  "cluster-env": {
    "properties": {
      "metrics_collector_external_hosts": "{{{ sharedService.datalakeClusterManagerFqdn }}}"
    }
  }
{{{/if-true}}}
}