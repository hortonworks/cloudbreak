{
{{{#if-true sharedService.attachedCluster}}}
  "yarn-site": {
    "properties_attributes": {},
    "properties": {
      "yarn.timeline-service.reader.webapp.address": "{{{ sharedService.datalakeClusterManagerFqdn }}}:8198",
      "yarn.timeline-service.reader.webapp.https.address": "{{{ sharedService.datalakeClusterManagerFqdn }}}:8199",
      "yarn.log.server.web-service.url": "http://{{{ sharedService.datalakeClusterManagerFqdn }}}:8198/ws/v2/applicationlog",
      "yarn.resourcemanager.cluster-id": "{{{ general.uuid }}}"
    }
  },
  "yarn-hbase-site": {
    "properties_attributes": {},
    "properties": {
      "hbase.zookeeper.quorum": "{{{ sharedService.datalakeClusterManagerFqdn }}}"
    }
  }
{{{/if-true}}}
}