server:
  contextPath: "/as"

cb:
  server:
    contextPath: "/cb"

spring:
  cloud:
    consul:
      host: consul.service.consul
  freemarker:
    template-loader-path: classpath:/
    prefer-file-system-access: false