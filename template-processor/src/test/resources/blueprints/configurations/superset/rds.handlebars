{
  "superset-env": {
    "properties": {
      "superset_admin_password": "{{{ cluster_admin_password }}}",
      "superset_admin_firstname": "{{{ cluster_admin_firstname }}}",
      "superset_admin_lastname": "{{{ cluster_admin_lastname }}}",
      "superset_admin_email": "{{{ admin_email }}}"
    }
  }{{{#if rds.superset}}},
  "druid-common": {
    "properties": {
      "druid.metadata.storage.type": "{{{ rds.superset.subprotocol }}}",
      "druid.metadata.storage.connector.connectURI": "{{{ rds.superset.connectionURL }}}",
      "druid.metadata.storage.connector.user": "{{{ rds.superset.connectionUserName }}}",
      "druid.metadata.storage.connector.password": "{{{ rds.superset.connectionPassword }}}"
    }
  }{{{/if}}}
}